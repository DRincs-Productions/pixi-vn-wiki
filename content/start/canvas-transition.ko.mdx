---
title: 전환
description: 디졸브, 페이드, 무브, 푸시, 줌 전환을 비롯해 Pixi’VN이 canvas 컴포넌트를 표시하고 제거하기 위해 제공하는 내장 전환 효과에 대해 알아보세요. 게임 흐름에 고급 시각 효과를 적용하기 위해 전환을 커스터마이즈하거나 직접 만드는 방법을 알아보세요.
---

Pixi’VN은 <DynamicLink href="/start/canvas-components">canvas 컴포넌트</DynamicLink>를 표시하거나 제거하기 위한 다양한 전환 효과를 제공하며, [나만의 전환 만들기](#custom-functionality) 기능도 제공합니다.

The examples below use the `transitions` namespace introduced in the upcoming **v1.9.5**.
Import it from `@drincs/pixi-vn` or `@drincs/pixi-vn/canvas`. In v1.9.4, use the individual
function exports instead; those exports remain available but deprecated in v1.9.5.

<Accordions>

<Accordion title="디졸브" id="dissolve">

디졸브 전환:

* 컴포넌트를 표시할 때 `alpha`를 점차 증가시킵니다. 동일한 alias를 가진 컴포넌트가 존재하는 경우, 새 컴포넌트의 전환이 완료되면 기존 컴포넌트가 제거됩니다.
* 컴포넌트를 제거할 때 `alpha`를 점차 감소시킵니다.

The [`transitions.showWithDissolve`](/jsdoc/pixi-vn/index/namespaces/transitions/functions/showWithDissolve) function displays a canvas element with a dissolve transition.

The [`transitions.removeWithDissolve`](/jsdoc/pixi-vn/index/namespaces/transitions/functions/removeWithDissolve) function removes a canvas element with a dissolve transition.

```ts tab="content/labels/start.label.ts"
import { newLabel, transitions } from "@drincs/pixi-vn";

export const startLabel = newLabel("start", [
    async () => {
        await transitions.showWithDissolve("alien", "egg_head");
        await transitions.showWithDissolve("human", {
            value: ["m01-body", "m01-eyes", "m01-mouth"],
            options: { scale: 0.5, xAlign: 0.7 },
        });
    },
    async () => {
        await transitions.showWithDissolve("alien", "flower_top");
        transitions.removeWithDissolve("human");
    },
]);
```

```ts tab="assets/index.ts"
import { AssetsManifest } from "@drincs/pixi-vn";

/**
 * Manifest for the assets used in the game.
 * You can read more about the manifest here: https://pixijs.com/8.x/guides/components/assets#loading-multiple-assets
 */
export const manifest: AssetsManifest = {
    bundles: [
        {
            name: "start",
            assets: [
                {
                    alias: "egg_head",
                    src: "https://pixijs.com/assets/eggHead.png",
                },
                {
                    alias: "flower_top",
                    src: "https://pixijs.com/assets/flowerTop.png",
                },
                {
                    alias: "m01-body",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-body.webp",
                },
                {
                    alias: "m01-eyes",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-eyes-smile.webp",
                },
                {
                    alias: "m01-mouth",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-mouth-smile00.webp",
                },
            ],
        },
    ],
};
```

<PixiVnExample path="canvas/dissolve-transition" />

</Accordion>

<Accordion title="페이드" id="fade">

페이드 전환:

* 컴포넌트를 표시할 때 `alpha`를 점차 증가시킵니다. 동일한 alias를 가진 컴포넌트가 존재하는 경우, 새 컴포넌트가 표시되기 전에 기존 컴포넌트가 페이드 아웃 효과와 함께 제거됩니다.
* 컴포넌트를 제거할 때 `alpha`를 점차 감소시킵니다.

The [`transitions.showWithFade`](/jsdoc/pixi-vn/index/namespaces/transitions/functions/showWithFade) function displays a canvas element with a fade transition.

The [`transitions.removeWithFade`](/jsdoc/pixi-vn/index/namespaces/transitions/functions/removeWithFade) function removes a canvas element with a fade transition.

```ts tab="content/labels/start.label.ts"
import { newLabel, transitions } from "@drincs/pixi-vn";

export const startLabel = newLabel("start", [
    async () => {
        await transitions.showWithFade("alien", "egg_head", { duration: 5 });
        await transitions.showWithFade("human", {
            value: ["m01-body", "m01-eyes", "m01-mouth"],
            options: { scale: 0.5, xAlign: 0.7 },
        });
    },
    async () => {
        await transitions.showWithFade("alien", "flower_top");
        transitions.removeWithFade("human");
    },
]);
```

```ts tab="assets/index.ts"
import { AssetsManifest } from "@drincs/pixi-vn";

/**
 * Manifest for the assets used in the game.
 * You can read more about the manifest here: https://pixijs.com/8.x/guides/components/assets#loading-multiple-assets
 */
export const manifest: AssetsManifest = {
    bundles: [
        {
            name: "start",
            assets: [
                {
                    alias: "egg_head",
                    src: "https://pixijs.com/assets/eggHead.png",
                },
                {
                    alias: "flower_top",
                    src: "https://pixijs.com/assets/flowerTop.png",
                },
                {
                    alias: "m01-body",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-body.webp",
                },
                {
                    alias: "m01-eyes",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-eyes-smile.webp",
                },
                {
                    alias: "m01-mouth",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-mouth-smile00.webp",
                },
            ],
        },
    ],
};
```

<PixiVnExample path="canvas/fade-transition" />

</Accordion>

<Accordion title="무브 인/아웃" id="move-in-out">

무브 인/아웃 전환:

* 컴포넌트를 표시할 때, (오른쪽, 왼쪽, 위쪽, 아래쪽 등) 바깥에서 canvas 상의 위치로 이동시킵니다. 동일한 alias를 가진 컴포넌트가 존재하는 경우, 새 컴포넌트가 표시되기 전에 기존 컴포넌트가 무브 아웃 효과와 함께 제거됩니다.
* 컴포넌트를 제거할 때, 해당 위치에서 canvas 바깥으로 이동시킵니다.

The [`transitions.moveIn`](/jsdoc/pixi-vn/index/namespaces/transitions/functions/moveIn) function displays a canvas element with a move-in transition.

The [`transitions.moveOut`](/jsdoc/pixi-vn/index/namespaces/transitions/functions/moveOut) function removes a canvas element with a move-out transition.

```ts tab="content/labels/start.label.ts"
import { newLabel, transitions } from "@drincs/pixi-vn";

export const startLabel = newLabel("start", [
    async () => {
        await transitions.moveIn("alien", "egg_head", { direction: "up" });
        await transitions.moveIn("human", {
            value: ["m01-body", "m01-eyes", "m01-mouth"],
            options: { scale: 0.5, xAlign: 0.7 },
        });
    },
    async () => {
        await transitions.moveIn("alien", "flower_top", { direction: "up" });
        transitions.moveOut("human");
    },
]);
```

```ts tab="assets/index.ts"
import { AssetsManifest } from "@drincs/pixi-vn";

/**
 * Manifest for the assets used in the game.
 * You can read more about the manifest here: https://pixijs.com/8.x/guides/components/assets#loading-multiple-assets
 */
export const manifest: AssetsManifest = {
    bundles: [
        {
            name: "start",
            assets: [
                {
                    alias: "egg_head",
                    src: "https://pixijs.com/assets/eggHead.png",
                },
                {
                    alias: "flower_top",
                    src: "https://pixijs.com/assets/flowerTop.png",
                },
                {
                    alias: "m01-body",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-body.webp",
                },
                {
                    alias: "m01-eyes",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-eyes-smile.webp",
                },
                {
                    alias: "m01-mouth",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-mouth-smile00.webp",
                },
            ],
        },
    ],
};
```

<PixiVnExample path="canvas/movein-transition" />

</Accordion>

<Accordion title="푸시 인/아웃" id="push-in-out">

푸시 인/아웃 전환:

* 컴포넌트를 표시할 때, (오른쪽, 왼쪽, 위쪽, 아래쪽 등) 바깥에서 canvas 상의 위치로 이동시킵니다. 동일한 alias를 가진 컴포넌트가 존재하는 경우, 새 컴포넌트가 들어오는 동안 기존 컴포넌트가 푸시 아웃 효과와 함께 제거됩니다.
* 컴포넌트를 제거할 때, 해당 위치에서 canvas 바깥으로 이동시킵니다.

The [`transitions.pushIn`](/jsdoc/pixi-vn/index/namespaces/transitions/functions/pushIn) function displays a canvas element with a push-in transition.

The [`transitions.pushOut`](/jsdoc/pixi-vn/index/namespaces/transitions/functions/pushOut) function removes a canvas element with a push-out transition.

```ts tab="content/labels/start.label.ts"
import { newLabel, transitions } from "@drincs/pixi-vn";

export const startLabel = newLabel("start", [
    async () => {
        await transitions.pushIn("alien", "egg_head");
        await transitions.pushIn("human", {
            value: ["m01-body", "m01-eyes", "m01-mouth"],
            options: { scale: 0.5, xAlign: 0.7 },
        });
    },
    async () => {
        await transitions.pushIn("alien", "flower_top", { direction: "up" });
        transitions.pushOut("human");
    },
]);
```

```ts tab="assets/index.ts"
import { AssetsManifest } from "@drincs/pixi-vn";

/**
 * Manifest for the assets used in the game.
 * You can read more about the manifest here: https://pixijs.com/8.x/guides/components/assets#loading-multiple-assets
 */
export const manifest: AssetsManifest = {
    bundles: [
        {
            name: "start",
            assets: [
                {
                    alias: "egg_head",
                    src: "https://pixijs.com/assets/eggHead.png",
                },
                {
                    alias: "flower_top",
                    src: "https://pixijs.com/assets/flowerTop.png",
                },
                {
                    alias: "m01-body",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-body.webp",
                },
                {
                    alias: "m01-eyes",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-eyes-smile.webp",
                },
                {
                    alias: "m01-mouth",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-mouth-smile00.webp",
                },
            ],
        },
    ],
};
```

<PixiVnExample path="canvas/pushin-transition" />

</Accordion>

<Accordion title="줌 인/아웃" id="zoom-in-out">

줌 인/아웃 전환:

* 컴포넌트를 표시할 때, (canvas 바깥에서) 크기 0에서 원래 크기와 위치로 확대시킵니다. 동일한 alias를 가진 컴포넌트가 존재하는 경우, 새 컴포넌트의 전환이 완료되면 기존 컴포넌트가 제거됩니다.
* 컴포넌트를 제거할 때, 원래 크기에서 0으로 축소시키며 canvas 바깥으로 이동시킵니다.

The [`transitions.zoomIn`](/jsdoc/pixi-vn/index/namespaces/transitions/functions/zoomIn) function displays a canvas element with a zoom-in transition.

The [`transitions.zoomOut`](/jsdoc/pixi-vn/index/namespaces/transitions/functions/zoomOut) function removes a canvas element with a zoom-out transition.

```ts tab="content/labels/start.label.ts"
import { newLabel, transitions } from "@drincs/pixi-vn";

export const startLabel = newLabel("start", [
    async () => {
        await transitions.zoomIn("alien", "egg_head");
        await transitions.zoomIn("human", {
            value: ["m01-body", "m01-eyes", "m01-mouth"],
            options: { scale: 0.5, xAlign: 0.7 },
        });
    },
    async () => {
        await transitions.zoomIn("alien", "flower_top");
        transitions.zoomOut("human");
    },
]);
```

```ts tab="assets/index.ts"
import { AssetsManifest } from "@drincs/pixi-vn";

/**
 * Manifest for the assets used in the game.
 * You can read more about the manifest here: https://pixijs.com/8.x/guides/components/assets#loading-multiple-assets
 */
export const manifest: AssetsManifest = {
    bundles: [
        {
            name: "start",
            assets: [
                {
                    alias: "egg_head",
                    src: "https://pixijs.com/assets/eggHead.png",
                },
                {
                    alias: "flower_top",
                    src: "https://pixijs.com/assets/flowerTop.png",
                },
                {
                    alias: "m01-body",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-body.webp",
                },
                {
                    alias: "m01-eyes",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-eyes-smile.webp",
                },
                {
                    alias: "m01-mouth",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-mouth-smile00.webp",
                },
            ],
        },
    ],
};
```

<PixiVnExample path="canvas/zoomin-transition" />

</Accordion>

<Accordion title="Wipe in/out" id="wipe-in-out">

The wipe in/out transition reveals or conceals a component using a moving boundary. It is a single generic primitive rather than separate `wipeLeft`/`wipeRight` functions: the boundary's `angle` (or a `direction` shorthand), edge `softness`, and `invert` are all configurable, so the same primitive can produce horizontal, vertical, diagonal, hard-edged, or soft-edged reveals.

* When showing a component, progressively reveals it as the boundary sweeps across. 동일한 alias를 가진 컴포넌트가 존재하는 경우, 새 컴포넌트의 전환이 완료되면 기존 컴포넌트가 제거됩니다.
* When removing a component, progressively conceals it as the boundary sweeps across.

The [`transitions.wipeIn`](/jsdoc/pixi-vn/index/namespaces/transitions/functions/wipeIn) function displays a canvas element with a wipe-in transition.

The [`transitions.wipeOut`](/jsdoc/pixi-vn/index/namespaces/transitions/functions/wipeOut) function removes a canvas element with a wipe-out transition.

```ts tab="content/labels/start.label.ts"
import { newLabel, transitions } from "@drincs/pixi-vn";

export const startLabel = newLabel("start", [
    async () => {
        await transitions.wipeIn("alien", "egg_head");
        await transitions.wipeIn("human", {
            value: ["m01-body", "m01-eyes", "m01-mouth"],
            options: { scale: 0.5, xAlign: 0.7 },
        });
    },
    async () => {
        await transitions.wipeIn("alien", "flower_top", { angle: 45, softness: 30 });
        transitions.wipeOut("human");
    },
]);
```

```ts tab="assets/index.ts"
import { AssetsManifest } from "@drincs/pixi-vn";

/**
 * Manifest for the assets used in the game.
 * You can read more about the manifest here: https://pixijs.com/8.x/guides/components/assets#loading-multiple-assets
 */
export const manifest: AssetsManifest = {
    bundles: [
        {
            name: "start",
            assets: [
                {
                    alias: "egg_head",
                    src: "https://pixijs.com/assets/eggHead.png",
                },
                {
                    alias: "flower_top",
                    src: "https://pixijs.com/assets/flowerTop.png",
                },
                {
                    alias: "m01-body",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-body.webp",
                },
                {
                    alias: "m01-eyes",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-eyes-smile.webp",
                },
                {
                    alias: "m01-mouth",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-mouth-smile00.webp",
                },
            ],
        },
    ],
};
```

<PixiVnExample path="canvas/wipein-transition" />

</Accordion>

<Accordion title="Iris in/out" id="iris-in-out">

The iris in/out transition reveals or conceals a component using an expanding or contracting radial mask. A centered circular iris is only one configuration: moving `origin` off-center (normalized to the component's own bounds) makes the same primitive useful as a focus/reveal effect centered on a character, and `aspect` turns the circle into a wide or tall ellipse.

* When showing a component, the mask expands from `origin` to progressively reveal it. 동일한 alias를 가진 컴포넌트가 존재하는 경우, 새 컴포넌트의 전환이 완료되면 기존 컴포넌트가 제거됩니다.
* When removing a component, the mask contracts back towards `origin` to progressively conceal it.

The [`transitions.irisIn`](/jsdoc/pixi-vn/index/namespaces/transitions/functions/irisIn) function displays a canvas element with an iris-in transition.

The [`transitions.irisOut`](/jsdoc/pixi-vn/index/namespaces/transitions/functions/irisOut) function removes a canvas element with an iris-out transition.

```ts tab="content/labels/start.label.ts"
import { newLabel, transitions } from "@drincs/pixi-vn";

export const startLabel = newLabel("start", [
    async () => {
        await transitions.irisIn("alien", "egg_head");
        await transitions.irisIn("human", {
            value: ["m01-body", "m01-eyes", "m01-mouth"],
            options: { scale: 0.5, xAlign: 0.7 },
        });
    },
    async () => {
        await transitions.irisIn("alien", "flower_top", {
            origin: { x: 0.2, y: 0.8 },
            softness: 25,
        });
        transitions.irisOut("human");
    },
]);
```

```ts tab="assets/index.ts"
import { AssetsManifest } from "@drincs/pixi-vn";

/**
 * Manifest for the assets used in the game.
 * You can read more about the manifest here: https://pixijs.com/8.x/guides/components/assets#loading-multiple-assets
 */
export const manifest: AssetsManifest = {
    bundles: [
        {
            name: "start",
            assets: [
                {
                    alias: "egg_head",
                    src: "https://pixijs.com/assets/eggHead.png",
                },
                {
                    alias: "flower_top",
                    src: "https://pixijs.com/assets/flowerTop.png",
                },
                {
                    alias: "m01-body",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-body.webp",
                },
                {
                    alias: "m01-eyes",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-eyes-smile.webp",
                },
                {
                    alias: "m01-mouth",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-mouth-smile00.webp",
                },
            ],
        },
    ],
};
```

<PixiVnExample path="canvas/irisin-transition" />

</Accordion>

<Accordion title="Split in/out" id="split-in-out">

The split in/out transition reveals or conceals a component using two mask panels that move apart or together. It covers "curtain"-like effects without a dedicated, story-specific API: `orientation` picks whether the panels split vertically or horizontally, and `origin` moves the split line off-center.

* When showing a component, the two panels slide together from the edges to progressively reveal it, meeting at the split line once fully shown. 동일한 alias를 가진 컴포넌트가 존재하는 경우, 새 컴포넌트의 전환이 완료되면 기존 컴포넌트가 제거됩니다.
* When removing a component, the two panels retract apart towards the edges to progressively conceal it.

The [`transitions.splitIn`](/jsdoc/pixi-vn/index/namespaces/transitions/functions/splitIn) function displays a canvas element with a split-in transition.

The [`transitions.splitOut`](/jsdoc/pixi-vn/index/namespaces/transitions/functions/splitOut) function removes a canvas element with a split-out transition.

```ts tab="content/labels/start.label.ts"
import { newLabel, transitions } from "@drincs/pixi-vn";

export const startLabel = newLabel("start", [
    async () => {
        await transitions.splitIn("alien", "egg_head");
        await transitions.splitIn("human", {
            value: ["m01-body", "m01-eyes", "m01-mouth"],
            options: { scale: 0.5, xAlign: 0.7 },
        });
    },
    async () => {
        await transitions.splitIn("alien", "flower_top", {
            orientation: "horizontal",
            origin: 0.25,
        });
        transitions.splitOut("human");
    },
]);
```

```ts tab="assets/index.ts"
import { AssetsManifest } from "@drincs/pixi-vn";

/**
 * Manifest for the assets used in the game.
 * You can read more about the manifest here: https://pixijs.com/8.x/guides/components/assets#loading-multiple-assets
 */
export const manifest: AssetsManifest = {
    bundles: [
        {
            name: "start",
            assets: [
                {
                    alias: "egg_head",
                    src: "https://pixijs.com/assets/eggHead.png",
                },
                {
                    alias: "flower_top",
                    src: "https://pixijs.com/assets/flowerTop.png",
                },
                {
                    alias: "m01-body",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-body.webp",
                },
                {
                    alias: "m01-eyes",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-eyes-smile.webp",
                },
                {
                    alias: "m01-mouth",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-mouth-smile00.webp",
                },
            ],
        },
    ],
};
```

<PixiVnExample path="canvas/splitin-transition" />

</Accordion>

<Accordion title="Flash in/out" id="flash-in-out">

The flash in/out transition plays a configurable solid-color overlay that fades in, optionally holds, and fades back out — not limited to a white flash. White reads as a camera/explosion-like flash, black behaves like a blink/cut, and arbitrary colors work for damage, magic, memory, or UI transitions. `pulses` repeats the fade-in/hold/fade-out cycle for a multi-flash effect (e.g. a damage flash).

* When showing a component, the new component appears immediately and the overlay plays over it.
* When removing a component, the overlay plays over it and it is then removed.

The [`transitions.flashIn`](/jsdoc/pixi-vn/index/namespaces/transitions/functions/flashIn) function displays a canvas element with a flash-in transition.

The [`transitions.flashOut`](/jsdoc/pixi-vn/index/namespaces/transitions/functions/flashOut) function removes a canvas element with a flash-out transition.

```ts tab="content/labels/start.label.ts"
import { newLabel, transitions } from "@drincs/pixi-vn";

export const startLabel = newLabel("start", [
    async () => {
        await transitions.flashIn("alien", "egg_head");
        await transitions.flashIn("human", {
            value: ["m01-body", "m01-eyes", "m01-mouth"],
            options: { scale: 0.5, xAlign: 0.7 },
        });
    },
    async () => {
        await transitions.flashIn("alien", "flower_top", {
            color: 0xff0033,
            holdDuration: 0.05,
            pulses: 3,
        });
        transitions.flashOut("human", { color: 0x000000 });
    },
]);
```

```ts tab="assets/index.ts"
import { AssetsManifest } from "@drincs/pixi-vn";

/**
 * Manifest for the assets used in the game.
 * You can read more about the manifest here: https://pixijs.com/8.x/guides/components/assets#loading-multiple-assets
 */
export const manifest: AssetsManifest = {
    bundles: [
        {
            name: "start",
            assets: [
                {
                    alias: "egg_head",
                    src: "https://pixijs.com/assets/eggHead.png",
                },
                {
                    alias: "flower_top",
                    src: "https://pixijs.com/assets/flowerTop.png",
                },
                {
                    alias: "m01-body",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-body.webp",
                },
                {
                    alias: "m01-eyes",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-eyes-smile.webp",
                },
                {
                    alias: "m01-mouth",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-mouth-smile00.webp",
                },
            ],
        },
    ],
};
```

<PixiVnExample path="canvas/flashin-transition" />

</Accordion>

<Accordion title="Blur in/out" id="blur-in-out">

The blur in/out transition animates blur strength into or out of focus. It is a generic blur, not a "dream"/"flashback" transition specifically — combine it with `showWithFade` for that recipe (see [custom functionality](#custom-functionality) below for building recipes).

* When showing a component, it appears already blurred and sharpens into focus. 동일한 alias를 가진 컴포넌트가 존재하는 경우, 새 컴포넌트의 전환이 완료되면 기존 컴포넌트가 제거됩니다.
* When removing a component, it blurs out of focus before being removed.

The [`transitions.blurIn`](/jsdoc/pixi-vn/index/namespaces/transitions/functions/blurIn) function displays a canvas element with a blur-in transition.

The [`transitions.blurOut`](/jsdoc/pixi-vn/index/namespaces/transitions/functions/blurOut) function removes a canvas element with a blur-out transition.

```ts tab="content/labels/start.label.ts"
import { newLabel, transitions } from "@drincs/pixi-vn";

export const startLabel = newLabel("start", [
    async () => {
        await transitions.blurIn("alien", "egg_head");
        await transitions.blurIn("human", {
            value: ["m01-body", "m01-eyes", "m01-mouth"],
            options: { scale: 0.5, xAlign: 0.7 },
        });
    },
    async () => {
        await transitions.blurIn("alien", "flower_top", { strength: 60, quality: 6 });
        transitions.blurOut("human");
    },
]);
```

```ts tab="assets/index.ts"
import { AssetsManifest } from "@drincs/pixi-vn";

/**
 * Manifest for the assets used in the game.
 * You can read more about the manifest here: https://pixijs.com/8.x/guides/components/assets#loading-multiple-assets
 */
export const manifest: AssetsManifest = {
    bundles: [
        {
            name: "start",
            assets: [
                {
                    alias: "egg_head",
                    src: "https://pixijs.com/assets/eggHead.png",
                },
                {
                    alias: "flower_top",
                    src: "https://pixijs.com/assets/flowerTop.png",
                },
                {
                    alias: "m01-body",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-body.webp",
                },
                {
                    alias: "m01-eyes",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-eyes-smile.webp",
                },
                {
                    alias: "m01-mouth",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-mouth-smile00.webp",
                },
            ],
        },
    ],
};
```

<PixiVnExample path="canvas/blurin-transition" />

</Accordion>

<Accordion title="Pixelate in/out" id="pixelate-in-out">

The pixelate in/out transition animates pixelation strength into or out of focus. Useful for retro effects, digital transitions, censorship/stylization, or scene changes — not only "glitch" scenes.

* When showing a component, it appears already pixelated and resolves into focus. 동일한 alias를 가진 컴포넌트가 존재하는 경우, 새 컴포넌트의 전환이 완료되면 기존 컴포넌트가 제거됩니다.
* When removing a component, it pixelates into blocks before being removed.

The [`transitions.pixelateIn`](/jsdoc/pixi-vn/index/namespaces/transitions/functions/pixelateIn) function displays a canvas element with a pixelate-in transition.

The [`transitions.pixelateOut`](/jsdoc/pixi-vn/index/namespaces/transitions/functions/pixelateOut) function removes a canvas element with a pixelate-out transition.

```ts tab="content/labels/start.label.ts"
import { newLabel, transitions } from "@drincs/pixi-vn";

export const startLabel = newLabel("start", [
    async () => {
        await transitions.pixelateIn("alien", "egg_head");
        await transitions.pixelateIn("human", {
            value: ["m01-body", "m01-eyes", "m01-mouth"],
            options: { scale: 0.5, xAlign: 0.7 },
        });
    },
    async () => {
        await transitions.pixelateIn("alien", "flower_top", { pixelSize: 64 });
        transitions.pixelateOut("human");
    },
]);
```

```ts tab="assets/index.ts"
import { AssetsManifest } from "@drincs/pixi-vn";

/**
 * Manifest for the assets used in the game.
 * You can read more about the manifest here: https://pixijs.com/8.x/guides/components/assets#loading-multiple-assets
 */
export const manifest: AssetsManifest = {
    bundles: [
        {
            name: "start",
            assets: [
                {
                    alias: "egg_head",
                    src: "https://pixijs.com/assets/eggHead.png",
                },
                {
                    alias: "flower_top",
                    src: "https://pixijs.com/assets/flowerTop.png",
                },
                {
                    alias: "m01-body",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-body.webp",
                },
                {
                    alias: "m01-eyes",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-eyes-smile.webp",
                },
                {
                    alias: "m01-mouth",
                    src: "https://raw.githubusercontent.com/DRincs-Productions/pixi-vn-bucket/refs/heads/main/breakdown/m01/m01-mouth-smile00.webp",
                },
            ],
        },
    ],
};
```

<PixiVnExample path="canvas/pixelatein-transition" />

</Accordion>

</Accordions>

## 사용자 정의 기능

<Callout type="info">

Pixi’VN 팀은 이 라이브러리를 더욱 완성도 있게 만들기 위한 새로운 제안과 기여를 환영합니다. 아래 채팅에서 여러분의 전환을 자유롭게 공유하거나 제안해 주세요!

</Callout>

나만의 전환을 만드는 것은 간단합니다. <DynamicLink href="/start/canvas-motion">`canvas.animate`</DynamicLink>를 사용하여 커스텀 효과를 정의하세요.

To help you get started, here is a simplified version of [`transitions.showWithDissolve`](/jsdoc/pixi-vn/index/namespaces/transitions/functions/showWithDissolve):

```ts title="canvas/transitions/showWithDissolve.ts"
import { canvas, ImageSprite, UPDATE_PRIORITY } from "@drincs/pixi-vn";
import { AnimationOptions } from "@drincs/pixi-vn/motion";

export default async function showWithDissolve(
    alias: string,
    component: ImageSprite,
    props: AnimationOptions = {},
    priority?: UPDATE_PRIORITY,
): Promise<string[] | undefined> {
    let { completeOnContinue = true, ...options } = props;
    // add the new component
    canvas.add(alias, component);
    // edit the properties of the new component
    component.alpha = 0;
    // create the ticker and play it
    let id = canvas.animate(
        alias,
        {
            alpha: 1,
        },
        {
            ...options,
            completeOnContinue,
        },
        priority,
    );
    // load the image if the image is not loaded
    if (component.haveEmptyTexture) {
        await component.load();
    }
    // return the ids of the tickers
    if (id) {
        return [id];
    }
}
```

<Accordions>

<Accordion title="Using filters" id="using-filters">

`canvas.animate` (above) animates a canvas component's own properties - `alpha`, position, scale, and
so on. Some effects (blur, pixelate, glow, color grading, ...) instead need to animate a PixiJS
<DynamicLink href="/start/canvas-filters">`Filter`</DynamicLink>'s own properties over time - that's
what `filters.animate` (from `@drincs/pixi-vn/filters`) is for. **This functionality is available
starting from v1.9.4.** It's the same mechanism `transitions.blurIn`/`blurOut`/`pixelateIn`/`pixelateOut`
use internally.

`filters.animate` mirrors `canvas.animate`'s `(alias, keyframes, options, priority)` shape, with the
`Filter` instance to animate inserted as the second argument. You're responsible for attaching the
filter to the component before starting the animation, and detaching (and destroying) it once the
animation completes, via the `cleanup` callback:

```ts title="canvas/transitions/blurIn.ts"
import { canvas, ImageSprite, UPDATE_PRIORITY } from "@drincs/pixi-vn";
import { filters } from "@drincs/pixi-vn/filters";
import { AnimationOptions } from "@drincs/pixi-vn/motion";

export default async function blurIn(
    alias: string,
    component: ImageSprite,
    props: AnimationOptions & { strength?: number } = {},
    priority?: UPDATE_PRIORITY,
): Promise<string[] | undefined> {
    let { strength = 32, completeOnContinue = true, ...options } = props;
    // add the new component
    canvas.add(alias, component);
    // create the filter and attach it to the component
    const filter = new filters.BlurFilter({ strength });
    component.filters = [filter];
    // animate the filter's own `strength` property down to 0
    let id = filters.animate(
        alias,
        filter,
        {
            strength: [strength, 0],
        },
        {
            ...options,
            completeOnContinue,
        },
        priority,
        undefined,
        // cleanup: runs once the animation completes - detach and destroy the filter
        () => {
            component.filters = null;
            filter.destroy();
        },
    );
    // load the image if the image is not loaded
    if (component.haveEmptyTexture) {
        await component.load();
    }
    // return the ids of the tickers
    if (id) {
        return [id];
    }
}
```

`filters.animate` can also drive a plain numeric progress value instead of a `Filter`'s own property -
pass `undefined` as the filter and an `apply` callback (called every frame with the interpolated
value) in its place. This is how `transitions.wipeIn`/`irisIn`/`splitIn` animate their mask geometry
(a growing radius, a moving boundary, ...), since a mask has no filter property to write directly.

The <DynamicLink href="/start/canvas-filters">Filters</DynamicLink> page covers every filter Pixi’VN
registers out of the box, plus how to make a custom filter save-compatible.

</Accordion>

</Accordions>

### 이전 컴포넌트 교체 또는 제거

동일한 alias를 가진 컴포넌트가 이미 존재하는 경우, 이를 교체되도록 두거나 다음과 같이 할 수 있습니다:

<Accordions>

<Accordion title="완료 후 이전 컴포넌트 제거" id="remove-the-previous-component-after-completion">

새 컴포넌트의 전환이 완료되었을 때 이전 컴포넌트를 제거하려면 <DynamicLink href="/start/canvas-motion">`aliasToRemoveAfter` 속성</DynamicLink>을 사용하세요.

```ts title="canvas/transitions/showWithDissolve.ts"
import { canvas, ImageSprite, UPDATE_PRIORITY } from "@drincs/pixi-vn";
import { AnimationOptions } from "@drincs/pixi-vn/motion";

export default async function showWithDissolve(
    alias: string,
    component: ImageSprite,
    props: AnimationOptions = {},
    priority?: UPDATE_PRIORITY,
): Promise<string[] | undefined> {
    let { completeOnContinue = true, ...options } = props;
    // check if the alias is already exist // [!code ++]
    let oldComponentAlias: string | undefined = undefined; // [!code ++]
    let oldComponent = canvas.find(alias); // [!code ++]
    if (oldComponent) {
        // [!code ++]
        oldComponentAlias = alias + "_temp_disolve"; // [!code ++]
        canvas.editAlias(alias, oldComponentAlias); // [!code ++]
    } // [!code ++]
    // add the new component and transfer the properties of the old component to the new component
    canvas.add(alias, component);
    oldComponent?.parent?.setChildIndex(
        oldComponent,
        oldComponent.parent.getChildIndex(oldComponent) - 0.1,
    ); // [!code ++]
    oldComponentAlias &&
        canvas.copyCanvasElementProperty(oldComponentAlias, alias); // [!code ++]
    oldComponentAlias &&
        canvas.tickers.transfer(oldComponentAlias, alias, "duplicate"); // [!code ++]
    // edit the properties of the new component
    component.alpha = 0;
    // create the ticker and play it
    let id = canvas.animate(
        alias,
        {
            alpha: 1,
        },
        {
            ...options,
            completeOnContinue,
        },
        priority,
    );
    // load the image if the image is not loaded
    if (component.haveEmptyTexture) {
        await component.load();
    }
    // return the ids of the tickers
    if (id) {
        return [id];
    }
}
```

</Accordion>

<Accordion title="전환과 함께 이전 컴포넌트 제거" id="remove-the-previous-component-with-a-transition">

전환과 함께 이전 컴포넌트를 제거하려면 <DynamicLink href="/start/canvas-motion">`canvas.animate`</DynamicLink>로 또 다른 애니메이션을 실행하고 <DynamicLink href="/start/canvas-motion">`tickerIdToResume` 속성</DynamicLink>을 사용하세요.

```ts title="canvas/transitions/showWithFade.ts"
import { canvas, ImageSprite, UPDATE_PRIORITY } from "@drincs/pixi-vn";
import { AnimationOptions } from "@drincs/pixi-vn/motion";

export default async function showWithDissolve(
    alias: string,
    component: ImageSprite,
    props: AnimationOptions = {},
    priority?: UPDATE_PRIORITY,
): Promise<string[] | undefined> {
    let { completeOnContinue = true, ...options } = props;
    // check if the alias is already exist // [!code ++]
    let oldComponentAlias: string | undefined = undefined; // [!code ++]
    let oldComponent = canvas.find(alias); // [!code ++]
    if (oldComponent) {
        // [!code ++]
        return showWithDissolve(alias, component, props, priority); // [!code ++]
    } // [!code ++]
    let oldComponentAlias = alias + "_temp_fade"; // [!code ++]
    canvas.editAlias(alias, oldComponentAlias); // [!code ++]
    // add the new component and transfer the properties of the old component to the new component // [!code ++]
    canvas.add(alias, component);
    oldComponent?.parent?.setChildIndex(
        oldComponent,
        oldComponent.parent.getChildIndex(oldComponent) - 0.1,
    ); // [!code ++]
    oldComponentAlias &&
        canvas.copyCanvasElementProperty(oldComponentAlias, alias); // [!code ++]
    oldComponentAlias &&
        canvas.tickers.transfer(oldComponentAlias, alias, "duplicate"); // [!code ++]
    // edit the properties of the new component
    component.alpha = 0;
    // create the ticker and play it
    let id = canvas.animate(
        alias,
        {
            alpha: 1,
        },
        {
            ...options,
            completeOnContinue,
        },
        priority,
    );
    if (id) {
        // [!code ++]
        // pause the ticker // [!code ++]
        canvas.tickers.pause({ id: id }); // [!code ++]
        // remove the old component // [!code ++]
        canvas.animate(
            // [!code ++]
            alias, // [!code ++]
            {
                // [!code ++]
                alpha: 0, // [!code ++]
            }, // [!code ++]
            {
                // [!code ++]
                ...options, // [!code ++]
                tickerIdToResume: id, // [!code ++]
                aliasToRemoveAfter: oldComponentAlias, // [!code ++]
                completeOnContinue, // [!code ++]
            }, // [!code ++]
            priority, // [!code ++]
        ); // [!code ++]
    } // [!code ++]
    // load the image if the image is not loaded
    if (component.haveEmptyTexture) {
        await component.load();
    }
    // return the ids of the tickers
    if (id) {
        return [id];
    }
}
```

</Accordion>

</Accordions>
