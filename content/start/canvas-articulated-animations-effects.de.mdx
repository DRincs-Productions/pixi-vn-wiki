---
title: Artikulierte Animationen
description: Erfahren Sie, wie Sie artikulierte Animationen verwenden – Funktionen, die auf canvas.animate aufbauen, um komplexe Effekte wie Wackeln bei Pixi’VN-Canvas-Komponenten zu erzeugen.
---

import { ShakeExample } from "@/components/examples";

Artikulierte Animationen sind Funktionen, die die Funktion [`canvas.animate`](/jsdoc/pixi-vn/index/interfaces/CanvasManagerInterface#animate) verwenden, um Animationen zu erstellen, die auf Canvas-Komponenten angewendet werden können. Diese Funktionen werden typischerweise verwendet, um Effekte wie Wackeln, Springen oder andere komplexe Animationen zu erzeugen, die mehrere Schritte oder Komponenten umfassen.

<Accordions>

<Accordion title="Wackeln" id="shake">

Die Funktion [`shakeEffect`](/jsdoc/pixi-vn/index/functions/shakeEffect) ist eine artikulierte Animation, die eine Komponente wackeln lässt.

```ts tab="content/labels/start.label.ts"
import {
    CANVAS_APP_GAME_LAYER_ALIAS,
    newLabel,
    shakeEffect,
    showImage,
} from "@drincs/pixi-vn";

export const startLabel = newLabel("start", [
    async () => {
        await showImage("bg", "bg", { scale: 1.3 });
        await showImage("alien", "alien", { align: 0.5 });
        shakeEffect("alien"); // [!code focus]
    },
    async () => {
        shakeEffect(CANVAS_APP_GAME_LAYER_ALIAS); // [!code focus]
    },
]);
```

```ts tab="assets/index.ts"
import { AssetsManifest } from "@drincs/pixi-vn";

/**
 * Manifest for the assets used in the game.
 * You can read more about the manifest here: https://pixijs.com/8.x/guides/components/assets#loading-multiple-assets
 */
export const manifest: AssetsManifest = {
    bundles: [
        {
            name: "start",
            assets: [
                {
                    alias: "alien",
                    src: "https://pixijs.com/assets/eggHead.png",
                },
                {
                    alias: "bg",
                    src: "https://pixijs.com/assets/bg_grass.jpg",
                },
            ],
        },
    ],
};
```

<ShakeExample />

</Accordion>

</Accordions>
