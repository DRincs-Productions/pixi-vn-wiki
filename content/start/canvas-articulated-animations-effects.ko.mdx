---
title: 관절 애니메이션
description: canvas.animate를 기반으로 구축된 함수인 관절 애니메이션을 사용하여 Pixi’VN 캔버스 컴포넌트에서 흔들림과 같은 복잡한 효과를 만드는 방법을 알아보세요.
---

import { ShakeExample } from "@/components/examples";

관절 애니메이션은 [`canvas.animate`](/jsdoc/pixi-vn/index/interfaces/CanvasManagerInterface#animate) 함수를 사용하여 캔버스 컴포넌트에 적용할 수 있는 애니메이션을 만드는 함수입니다. 이러한 함수는 일반적으로 흔들림, 바운스 또는 여러 단계나 컴포넌트를 포함하는 기타 복잡한 애니메이션 효과를 만드는 데 사용됩니다.

<Accordions>

<Accordion title="흔들림" id="shake">

[`shakeEffect`](/jsdoc/pixi-vn/index/functions/shakeEffect) 함수는 컴포넌트를 흔드는 관절 애니메이션입니다.

```ts tab="content/labels/start.label.ts"
import {
    CANVAS_APP_GAME_LAYER_ALIAS,
    newLabel,
    shakeEffect,
    showImage,
} from "@drincs/pixi-vn";

export const startLabel = newLabel("start", [
    async () => {
        await showImage("bg", "bg", { scale: 1.3 });
        await showImage("alien", "alien", { align: 0.5 });
        shakeEffect("alien"); // [!code focus]
    },
    async () => {
        shakeEffect(CANVAS_APP_GAME_LAYER_ALIAS); // [!code focus]
    },
]);
```

```ts tab="assets/index.ts"
import { AssetsManifest } from "@drincs/pixi-vn";

/**
 * Manifest for the assets used in the game.
 * You can read more about the manifest here: https://pixijs.com/8.x/guides/components/assets#loading-multiple-assets
 */
export const manifest: AssetsManifest = {
    bundles: [
        {
            name: "start",
            assets: [
                {
                    alias: "alien",
                    src: "https://pixijs.com/assets/eggHead.png",
                },
                {
                    alias: "bg",
                    src: "https://pixijs.com/assets/bg_grass.jpg",
                },
            ],
        },
    ],
};
```

<ShakeExample />

</Accordion>

</Accordions>
