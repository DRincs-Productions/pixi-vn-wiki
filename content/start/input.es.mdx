---
title: Solicitud de entrada
description: Explica cómo solicitar al jugador que introduzca datos en Pixi’VN, incluyendo el uso, la recuperación y la eliminación de solicitudes de entrada.
---

import { InputPrompt } from "@/components/examples";

:::tip[Pantalla UI]

Puedes encontrar un ejemplo de la interfaz de diálogo de entrada en la sección de <DynamicLink href="/start/interface-examples#input-prompt">ejemplos de interfaz</DynamicLink>.

:::

:::tip[*ink*]

Puedes usar este método con la sintaxis *ink*. Consulta más información <DynamicLink href="/ink/input">aquí</DynamicLink>.

:::

En las novelas visuales, puede que necesites pedirle al jugador que introduzca texto, números, fechas u otros valores.

Pixi’VN proporciona funciones para gestionar las solicitudes de entrada. El desarrollador puede requerir que el jugador introduzca un valor (el juego no continuará hasta que se proporcione un valor), mientras que la <DynamicLink href="/start/interface">UI</DynamicLink> es responsable de mostrar la solicitud.

## Solicitar

To prompt the player for input, use the [`narration.input.request`](/jsdoc/pixi-vn/index/interfaces/NarrationInputInterface#request) function.

```ts title="content/labels/start.label.ts"
import { narration, newLabel } from "@drincs/pixi-vn";

export const startLabel = newLabel("start", [
    () => {
        narration.dialogue = "Hello";
    },
    () => {
        narration.dialogue = "What is your name?";
        narration.input.request({ type: "string" });
    },
    () => {
        narration.dialogue = `My name is ${narration.input.value}`;
    },
    () => {
        narration.dialogue = "How old are you?";
        narration.input.request({ type: "number" }, 18);
    },
    () => {
        narration.dialogue = `I am ${narration.input.value} years old`;
    },
    () => {
        narration.dialogue = "Describe who you are:";
        narration.input.request({ type: "html textarea" });
    },
    () => {
        narration.dialogue = `${narration.input.value}`;
    },
    () => {
        narration.dialogue = "Restart";
    },
]);
```

<InputPrompt />

## Obtener

Para obtener información sobre la solicitud de entrada, utiliza:

* [`narration.input.isRequired`](/jsdoc/pixi-vn/index/interfaces/NarrationInputInterface#isrequired)
* [`narration.input.type`](/jsdoc/pixi-vn/index/interfaces/NarrationInputInterface#type)

```ts
if (narration.input.isRequired) {
    openInputModal(narration.input.type);
}
```

## Eliminar

To remove the input prompt request, use [`narration.input.removeRequest`](/jsdoc/pixi-vn/index/interfaces/NarrationInputInterface#removerequest).

```ts
narration.input.removeRequest();
```
