---
title: Markdown
description: Pixi’VN에서 Markdown 사용 및 통합에 대한 개요. React, Vue, Svelte, Angular 예제 포함.
---

<Accordions>

<Accordion title="Markdown이란?" id="what-is-markdown">

**Markdown**은 일반 텍스트 편집기를 사용하여 서식 있는 텍스트를 만들기 위한 경량 마크업 언어입니다. John Gruber는 2004년에 Aaron Swartz와 협력하여 소스 코드 형태로도 읽기 쉬운 마크업 언어로 Markdown을 만들었습니다. Markdown은 블로그와 인스턴트 메시징에 널리 사용되며, 온라인 포럼, 협업 소프트웨어, 문서 페이지 및 readme 파일에서도 활용됩니다.

</Accordion>

</Accordions>

Pixi’VN은 어떤 마크업에도 종속되지 않으며, 개발자가 원하는 마크업을 선택할 수 있는 자유를 제공합니다. 그러나 Markdown을 사용하는 것을 권장합니다.

:::tip[템플릿]

모든 템플릿에서 Markdown 컴포넌트는 이미 정의되어 있습니다.

:::

:::tip[*ink*]

이 메서드는 *ink* 문법과 함께 사용할 수 있습니다. 자세한 내용은 <DynamicLink href="/ink/markup#markdown">여기</DynamicLink>를 참조하세요.

:::

다음은 JavaScript 생태계에서 Markdown을 구현하는 몇 가지 예제입니다:

```tsx tab="React"
// I use the react-markdown library to convert the Markdown to HTML
// read more about it here: https://www.npmjs.com/package/react-markdown
import Markdown from "react-markdown";
import rehypeRaw from "rehype-raw";
import remarkGfm from "remark-gfm";

export default function MarkdownComponent({ text }: { text: string }) {
    return (
        <Markdown remarkPlugins={[remarkGfm]} rehypePlugins={[rehypeRaw]}>
            {text}
        </Markdown>
    );
}
```

```vue tab="Vue"
<!-- I use the vue-markdown-render library to convert the Markdown to HTML -->
<!-- read more about it here: https://www.npmjs.com/package/vue-markdown-render -->
<template>
    <div>
        <vue-markdown :source="src" />
    </div>
</template>

<script lang="ts">
import VueMarkdown from 'vue-markdown-render'

export default defineComponent({
  name: 'MyComponent',
  components: {
    VueMarkdown
  },
  setup(props, ctx) {
    const src = ref('---
title: header')
---

    return {
      src
    }
  }
})
</script>
```

```svelte tab="Svelte"
<!-- I use the svelte-markdown library to convert the Markdown to HTML -->
<!-- read more about it here: https://www.npmjs.com/package/svelte-markdown -->
<script>
  import SvelteMarkdown from 'svelte-markdown'
  const source = `
  ---
title: This is a header
---

This is a paragraph.

* This is a list
* With two items
  1. And a sublist
  2. That is ordered
    * With another
    * Sublist inside

| And this is | A table |
|-------------|---------|
| With two    | columns |`
</script>

<SvelteMarkdown {source} />
```

```tsx tab="Angular"
// I use the ngx-markdown library to convert the Markdown to HTML
// read more about it here: https://www.npmjs.com/package/ngx-markdown
import { Component, Input } from "@angular/core";
import { MarkdownModule } from "ngx-markdown";

@Component({
    selector: "app-markdown",
    template: ` <markdown [data]="text"></markdown> `,
})
export class MarkdownComponent {
    @Input() text: string;
}
```

<Sandbox template="4h8wst" entry="/src/components/MarkdownComponent.tsx" previewHeight={300} />

## React Markdown Typewriter

[React Markdown Typewriter](https://www.npmjs.com/package/react-markdown-typewriter)는 Markdown과 타이프라이터 효과를 결합한 라이브러리입니다. 이 라이브러리는 React 템플릿의 Markdown 컴포넌트에 타이프라이터 효과를 추가해야 하는 저의 필요에 의해 제가 직접 만들었습니다.

React를 사용하고 있다면 이 라이브러리를 사용하는 것을 권장합니다:

```tsx title="React"
import { MarkdownTypewriter } from "react-markdown-typewriter";
import rehypeRaw from "rehype-raw";
import remarkGfm from "remark-gfm";

export default function MarkdownComponent({ text }: { text: string }) {
    return (
        <MarkdownTypewriter
            remarkPlugins={[remarkGfm]}
            rehypePlugins={[rehypeRaw]}
        >
            {text}
        </MarkdownTypewriter>
    );
}
```

<Sandbox template="rgjf6t" entry="/src/components/MarkdownComponent.tsx" previewHeight={320} />
