---
title: 저장소 클래스
description: |
  `StoredClassModel`을 사용하여 속성이 저장되는 클래스를 만드는 방법입니다.
---

Pixi’VN은 게임 저장소에 저장되는 속성을 가진 클래스를 만들 때 사용할 수 있는 추상 클래스 [`StoredClassModel`](/jsdoc/pixi-vn/index/classes/StoredClassModel)을 제공합니다.

```ts
const CITY_CATEGORY = "city";

export default class City extends StoredClassModel {
    constructor(id: string, props: CityProps) {
        super(CITY_CATEGORY, id);
        // ...
    }
}
```

```ts
const milan = new City("milan", {
    // ...
});
```

## 저장소 속성

클래스 속성을 게임 저장소에 저장하려면 [`getStorageProperty`](/jsdoc/pixi-vn/index/classes/StoredClassModel#getstorageproperty) 및 [`setStorageProperty`](/jsdoc/pixi-vn/index/classes/StoredClassModel#setstorageproperty) 헬퍼를 사용하세요.

```ts
export default class City extends StoredClassModel {
    constructor(id: string, props: CityProps) {
        // ...
    }

    get inhabitants(): number {
        return this.getStorageProperty<string>("inhabitants") || "";
    }
    set inhabitants(value: number) {
        this.setStorageProperty("inhabitants", value);
    }
}
```

```ts
const milan = new City("milan", {
    // ...
});

milan.inhabitants = 100000;
```
