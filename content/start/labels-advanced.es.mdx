---
title: Funcionalidades avanzadas de los labels
description: Explica las opciones avanzadas de label en Pixi'VN, como onLoadingLabel, onStepStart, onStepEnd y listas de steps dinámicas.
---

import { ReturningDifferentStepLists } from "@/components/examples";

Esta sección cubre las funcionalidades avanzadas de los <DynamicLink href="/start/labels">labels</DynamicLink>. No son necesarias para crear un juego básico, pero pueden ayudarte a crear experiencias más complejas e interactivas.

<Accordions>

<Accordion title="`onLoadingLabel`" id="onloadinglabel">

[`onLoadingLabel`](/jsdoc/pixi-vn/index/interfaces/LabelProps#onloadinglabel) es una función ejecutada en [`onStepStart`](/jsdoc/pixi-vn/index/interfaces/LabelProps#onstepstart) si el índice del `step` es 0, y también cuando el usuario carga una partida guardada.

Cuando se carga una partida guardada, se ejecutan todas las funciones [`onLoadingLabel`](/jsdoc/pixi-vn/index/interfaces/LabelProps#onloadinglabel) de los [`narration.openedLabels`](/jsdoc/pixi-vn/index/interfaces/NarrationManagerInterface#openedlabels) (el `label` actual y todos los `labels` en la pila).

Esto es útil, por ejemplo, para asegurarse de que todas las imágenes utilizadas en el `label` estén en caché antes de que el `label` comience.

Puedes establecer esta función pasándola a la propiedad [`onLoadingLabel`](/jsdoc/pixi-vn/index/interfaces/LabelProps#onloadinglabel) en las opciones del `label`.

```ts title="content/labels/start.label.ts"
const startLabel = newLabel(
    "start",
    [
        async () => {
            await showImage("image1", "path/to/image1.png");
            await showImage("image2", "path/to/image2.png");
        },
    ],
    {
        onLoadingLabel: async (stepIndex, label) => { // [!code focus]
            await Assets.load("path/to/image1.png"); // [!code focus]
            await Assets.load("path/to/image2.png"); // [!code focus]
        }, // [!code focus]
    },
);
```

También puedes usar [`Game.onLoadingLabel`](/jsdoc/pixi-vn/index/namespaces/Game/functions/onLoadingLabel) para interceptar la carga de todos los `labels` de forma global.

```ts
import { Game } from "@drincs/pixi-vn";

Game.onLoadingLabel((_stepId, label) => { // [!code focus]
    Assets.backgroundLoadBundle(label.id); // [!code focus]
}); // [!code focus]
```

</Accordion>

<Accordion title="`onStepStart`" id="onstepstart">

[`onStepStart`](/jsdoc/pixi-vn/index/interfaces/LabelProps#onstepstart) es una función ejecutada antes de cada `step` de un `label`. Establécela mediante la propiedad [`onStepStart`](/jsdoc/pixi-vn/index/interfaces/LabelProps#onstepstart) en las opciones del `label`.

```ts title="content/labels/start.label.ts"
const startLabel = newLabel(
    "start",
    [
        () => {
            narration.dialogue = "Step 1";
        },
        () => {
            narration.dialogue = "Step 2";
        },
    ],
    {
        onStepStart: (stepIndex, label) => { // [!code focus]
            console.log(`Step ${stepIndex} started`); // [!code focus]
        }, // [!code focus]
    },
);
```

También puedes usar [`Game.onStepStart`](/jsdoc/pixi-vn/index/namespaces/Game/functions/onStepStart) para interceptar el inicio de cualquier `step` en cualquier `label`.

```ts
import { Game } from "@drincs/pixi-vn";

Game.onStepStart((stepIndex, label) => { // [!code focus]
    console.log(`Step ${stepIndex} started`); // [!code focus]
}); // [!code focus]
```

</Accordion>

<Accordion title="`onStepEnd`" id="onstepend">

[`onStepEnd`](/jsdoc/pixi-vn/index/interfaces/LabelProps#onstepend) es una función ejecutada después de cada `step` de un `label`. Establécela mediante la propiedad [`onStepEnd`](/jsdoc/pixi-vn/index/interfaces/LabelProps#onstepend) en las opciones del `label`.

```ts title="content/labels/start.label.ts"
const startLabel = newLabel(
    "start",
    [
        () => {
            narration.dialogue = "Step 1";
        },
        () => {
            narration.dialogue = "Step 2";
        },
    ],
    {
        onStepEnd: (stepIndex, label) => { // [!code focus]
            console.log(`Step ${stepIndex} ended`); // [!code focus]
        }, // [!code focus]
    },
);
```

También puedes usar [`Game.onStepEnd`](/jsdoc/pixi-vn/index/namespaces/Game/functions/onStepEnd) para interceptar el final de cualquier `step` en cualquier `label`.

```ts
import { Game } from "@drincs/pixi-vn";

Game.onStepEnd((stepIndex, label) => { // [!code focus]
    console.log(`Step ${stepIndex} ended`); // [!code focus]
}); // [!code focus]
```

</Accordion>

<Accordion title="Lista de `step` dinámica" id="dynamic-step-list">

Al crear un nuevo `label`, puedes pasar una función que devuelva los `steps` para ese `label`. Esto te permite cambiar dinámicamente la lista de `steps` en función de una condición.

```ts title="content/labels/start.label.ts"
import { storage, narration, newLabel } from "@drincs/pixi-vn";

export const startLabel = newLabel("start", () => {
    let condition = storage.getFlag("condition");
    if (condition) {
        return [
            () => {
                narration.dialogue = "Step 2";
            },
            () => {
                narration.dialogue = "Restart";
            },
        ];
    } else {
        return [
            () => {
                narration.dialogue = "Step 1";
            },
            async (props, { labelId }) => {
                storage.setFlag("condition", true);
                return await narration.jump(labelId, props);
            },
        ];
    }
});
```

<ReturningDifferentStepLists />

</Accordion>

</Accordions>
