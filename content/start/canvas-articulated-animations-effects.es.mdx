---
title: Animaciones articuladas
description: Aprende a usar las animaciones articuladas, funciones basadas en canvas.animate para crear efectos complejos como el temblor en los componentes canvas de Pixi’VN.
---

import { ShakeExample } from "@/components/examples";

Las animaciones articuladas son funciones que utilizan la función [`canvas.animate`](/jsdoc/pixi-vn/index/interfaces/CanvasManagerInterface#animate) para crear animaciones que se pueden aplicar a los componentes canvas. Estas funciones se utilizan normalmente para crear efectos como el temblor, el rebote u otras animaciones complejas que involucran múltiples pasos o componentes.

<Accordions>

<Accordion title="Temblor" id="shake">

La función [`shakeEffect`](/jsdoc/pixi-vn/index/functions/shakeEffect) es una animación articulada que hace temblar a un componente.

```ts tab="content/labels/start.label.ts"
import {
    CANVAS_APP_GAME_LAYER_ALIAS,
    newLabel,
    shakeEffect,
    showImage,
} from "@drincs/pixi-vn";

export const startLabel = newLabel("start", [
    async () => {
        await showImage("bg", "bg", { scale: 1.3 });
        await showImage("alien", "alien", { align: 0.5 });
        shakeEffect("alien"); // [!code focus]
    },
    async () => {
        shakeEffect(CANVAS_APP_GAME_LAYER_ALIAS); // [!code focus]
    },
]);
```

```ts tab="assets/index.ts"
import { AssetsManifest } from "@drincs/pixi-vn";

/**
 * Manifest for the assets used in the game.
 * You can read more about the manifest here: https://pixijs.com/8.x/guides/components/assets#loading-multiple-assets
 */
export const manifest: AssetsManifest = {
    bundles: [
        {
            name: "start",
            assets: [
                {
                    alias: "alien",
                    src: "https://pixijs.com/assets/eggHead.png",
                },
                {
                    alias: "bg",
                    src: "https://pixijs.com/assets/bg_grass.jpg",
                },
            ],
        },
    ],
};
```

<ShakeExample />

</Accordion>

</Accordions>
