---
title: 템플릿
description: Learn how to generate a Pixi’VN project from a template, set it up in your IDE, run and customise it, and understand its project structure.
---

다음 명령을 실행하여 템플릿으로부터 새 프로젝트를 생성할 수 있습니다:

```npm
npm create pixi-vn@latest
```

## 인터랙티브 데모

<Accordions>

<Accordion title="비주얼 노벨" id="visual-novel">

<iframe
src="https://pixi-vn-visual-novel-example.pages.dev/demo"
title="비주얼 노벨"
style={{
      width: "100%",
      height: "400px",
      border: "0",
      borderRadius: "4px",
      overflow: "hidden",
  }}
allowFullScreen={true}
/>

</Accordion>

<Accordion title="포인트 앤 클릭" id="point-click-adventure">

<iframe
src="https://pixi-vn-point-and-click-example.pages.dev/demo"
title="포인트 앤 클릭"
style={{
      width: "100%",
      height: "400px",
      border: "0",
      borderRadius: "4px",
      overflow: "hidden",
  }}
allowFullScreen={true}
/>

</Accordion>

</Accordions>

## 템플릿 사용 방법

이 페이지를 읽고 있다면, 이미 템플릿으로부터 생성된 프로젝트를 가지고 있을 것입니다. 다음 섹션에서는 프로젝트를 실행하기 위한 첫 단계를 안내합니다.

### 초기 설정

IDE로 프로젝트 폴더를 여세요. 아래 예시에서는 [Visual Studio Code](https://code.visualstudio.com/)를 사용합니다.

프로젝트를 열면 VS Code가 자동으로 몇 가지를 제안합니다:

* **권장 확장 프로그램 설치** — 워크스페이스에서 권장하는 확장 프로그램을 설치하라는 알림이 표시됩니다. 이를 수락하는 것을 강력히 권장합니다. 알림이 표시되지 않는 경우, 확장 프로그램 패널에서 [권장 확장 프로그램](https://code.visualstudio.com/docs/editor/extension-marketplace#_recommended-extensions)을 필터링하여 수동으로 설치할 수 있습니다.
* **시작 작업 실행** — VS Code가 구성된 시작 작업을 실행하라는 메시지를 표시할 수 있으며, 이를 통해 모든 프로젝트 의존성이 설치됩니다. 수동으로 설치하려면 다음을 실행하세요:

```npm
npm install
```

:::warning[멀티플랫폼]

데스크톱 또는 모바일 플랫폼을 대상으로 하는 템플릿을 선택한 경우, [Tauri](https://v2.tauri.app/start/prerequisites/)에서 요구하는 추가 의존성(Rust 툴체인 및 플랫폼별 빌드 도구)을 설치해야 합니다. Tauri는 시스템 환경과 깊이 통합되므로, 프로젝트를 데스크톱 애플리케이션으로 실행할 수 있게 되기까지 **VS Code를 한 번 이상 재시작**해야 할 가능성이 높습니다.

:::

### 프로젝트 실행

템플릿에는 VS Code용으로 미리 구성된 **실행 구성(launch configurations)** 세트가 포함되어 있습니다. 익숙하지 않은 분들을 위해 설명하면: 실행 구성은 매번 터미널에 명령을 입력할 필요 없이 VS Code가 프로젝트를 시작하는 방법(그리고 선택적으로 디버거를 연결하는 방법)을 알려주는 바로가기입니다. **Run and Debug** 패널(왼쪽 사이드바의 재생 버튼 아이콘 또는 `F5` 키)에서 접근할 수 있습니다. 패널 상단의 드롭다운에서 원하는 구성을 선택하고 초록색 재생 버튼을 누르세요.

![Run-and-Debug-panel](/launch-configuration-dropdown.png)

다음 구성을 사용할 수 있습니다:

* **Debug Game**: 개발 중 가장 자주 사용하게 될 구성으로, 파일을 저장할 때마다 브라우저가 자동으로 새로고침됩니다. 이 모드에서는 애니메이션이 완벽하게 부드럽지 않을 수 있고, 에셋 로딩이 최종 게시 빌드보다 느릴 수 있다는 점에 유의하세요.
* **Debug Game Preview**: 게임이 게시된 후의 동작을 거의 그대로 시뮬레이션하는 구성입니다: 애니메이션이 부드럽고, 로딩 시간이 실제와 유사하며, 최적화가 적용됩니다. 게시하기 전에 모든 것이 올바르게 보이고 작동하는지 확인하고 싶을 때 이 구성을 사용하세요.
* **Run as Desktop App (Dev)**: Rust/Tauri 백엔드를 컴파일하고 게임을 네이티브 데스크톱 창으로 실행합니다. 템플릿이 데스크톱 또는 모바일 플랫폼을 대상으로 하지 않는 경우 이 구성은 존재하지 않습니다.

<Accordions>

<Accordion title="명령줄" id="command-line">

모든 명령은 프로젝트 루트에서 실행해야 합니다.

**개발**

| 명령어                         | 설명                                                       |
| --------------------------- | -------------------------------------------------------- |
| `npm run dev`               | Vite 개발 서버를 시작하고 브라우저에서 게임을 엽니다.                         |
| `npm run tauri:dev`         | 게임을 **데스크톱** 애플리케이션으로 시작합니다(멀티플랫폼 템플릿과 Tauri 설치가 필요합니다). |
| `npm run tauri:android:dev` | 연결된 Android 기기 또는 에뮬레이터에서 게임을 시작합니다.                     |
| `npm run tauri:ios:dev`     | 연결된 iOS 기기 또는 시뮬레이터에서 게임을 시작합니다.                         |

**빌드 및 미리보기**

| 명령어                  | 설명                                                          |
| -------------------- | ----------------------------------------------------------- |
| `npm run build:fast` | 웹 앱만 빌드합니다(Tauri 제외). 빠르며 웹 게시에 적합합니다.                      |
| `npm run build`      | 전체 빌드: TypeScript를 컴파일하고, 웹 앱을 빌드하며, Tauri 데스크톱 앱을 패키징합니다.  |
| `npm run preview`    | `build:fast`를 실행한 다음 그 결과를 로컬에서 제공합니다 — 게시된 웹 게임을 시뮬레이션합니다. |

**에셋 및 아이콘**

| 명령어            | 설명                                                                            |
| -------------- | ----------------------------------------------------------------------------- |
| `npm run icon` | `public/icon.png`에서 모든 아이콘 변형(PWA, 데스크톱, 모바일)을 생성합니다. 아이콘을 교체한 후 이 명령을 실행하세요. |

**사용자 인터페이스 (UI)**

| 명령어                 | 설명                                                                                       |
| ------------------- | ---------------------------------------------------------------------------------------- |
| `npm run ui:reinit` | [shadcn/ui](https://ui.shadcn.com/)를 처음부터 다시 초기화하고(모든 컴포넌트를 강제로 재설치) 이후 소스 파일을 다시 포맷합니다. |

**코드 품질**

| 명령어              | 설명                                                |
| ---------------- | ------------------------------------------------- |
| `npm run lint`   | [Biome](https://biomejs.dev/)을 사용하여 코드 오류를 확인합니다. |
| `npm run check`  | 모든 Biome 검사를 실행합니다(린트 + 포맷 검증).                   |
| `npm run format` | 모든 소스 파일을 자동으로 포맷합니다.                             |

</Accordion>

</Accordions>

### 스타일 사용자 지정

템플릿 사용자 인터페이스(UI)는 전적으로 [shadcn/ui](https://ui.shadcn.com/)로 구축되어 있으므로, 컴포넌트 코드를 건드리지 않고도 색상 테마를 사용자 지정할 수 있습니다.

[shadcn/ui 테마 빌더](https://ui.shadcn.com/create?template=vite\&base=base\&preset=b1s9eyGkS)를 열고 원하는 색상으로 조정한 다음, 페이지의 **Copy** 섹션으로 이동하여 생성된 CSS 명령을 복사하세요. 해당 명령을 프로젝트 루트에서 실행하세요:

```npm
npx shadcn@latest add <paste-the-generated-command-here>
```

적용한 후에는 소스 파일을 다시 포맷하는 것을 권장합니다:

```npm
npm run format
```

추가적인 사용자 지정 — 폰트, 전역 스타일 등 — 에 대해서는 <DynamicLink href="/start/interface-font">Styles & font</DynamicLink>을 참고하세요.

### 프로젝트 구조

<Files>
    <Folder name="project" defaultOpen>
        <Folder name="ink" defaultOpen>
            <File name="start.ink" />
        </Folder>
        <Folder name="public" defaultOpen>
            <File name="icon.png" />
        </Folder>
        <Folder name="src" defaultOpen>
            <Folder name="assets">
                <File name="manifest.gen.json" />
                <File name="ink-manifest.gen.json" />
                <File name="index.ts" />
            </Folder>
            <Folder name="components" />
            <Folder name="content" defaultOpen>
                <Folder name="ink">
                    <File name="hashtag-commands.ts" />
                    <File name="text-replaces.ts" />
                </Folder>
                <Folder name="labels">
                    <File name="start.label.ts" />
                </Folder>
                <File name="characters.ts" />
                <File name="index.ts" />
            </Folder>
            <Folder name="lib">
                <Folder name="hooks" />
                <Folder name="query" />
                <Folder name="stores" />
                <Folder name="utils" />
                <File name="i18n.ts" />
                <File name="steam.ts" />
            </Folder>
            <Folder name="locales">
                <File name="en.json" />
            </Folder>
            <Folder name="models">
                <File name="Character.ts" />
                <File name="GameSaveData.ts" />
            </Folder>
            <Folder name="routes">
                <Folder name="game">
                    <File name="narration.tsx" />
                </Folder>
                <File name="__root.tsx" />
                <File name="game.tsx" />
                <File name="index.tsx" />
            </Folder>
            <File name="constants.ts" />
            <File name="main.tsx" />
            <File name="pixi-vn.keys.gen.ts" />
            <File name="pixi-vn.d.ts" />
            <File name="styles.css" />
        </Folder>
        <Folder name="src-tauri" />
    </Folder>
</Files>

<Accordions>

<Accordion title="내레이션" id="project-structure-narration">

* `ink/` — 이곳에 ***ink*** 스크립트로 스토리를 작성하세요. 템플릿에는 시작을 위한 샘플 `start.ink`가 포함되어 있습니다.
* `src/content/` — 시작 시 자동으로 임포트됩니다; 여기에서 모든 게임 요소를 정의하세요:
  * `ink/hashtag-commands.ts` — ***ink*** 스크립트의 사용자 지정 해시태그 명령입니다. <DynamicLink href="/ink/hashtag">***ink*** 해시태그 명령</DynamicLink>을 참고하세요.
  * `ink/text-replaces.ts` — ***ink*** 대화의 텍스트 교체입니다. <DynamicLink href="/ink/replacement">***ink*** 텍스트 교체</DynamicLink>를 참고하세요.
  * `labels/` — 게임 흐름과 씬을 정의하는 내러티브 노드입니다(`.label.ts` 명명 규칙을 사용하세요).
  * `characters.ts` — 캐릭터 정의입니다.

</Accordion>

<Accordion title="에셋" id="project-structure-assets">

* `src/assets/` — 소스 게임 에셋(이미지, 오디오, 폰트 등)입니다. [Pixi Asset Pack](https://pixijs.io/assetpack/)에 의해 자동으로 분석됩니다.
* `public/assets/` — 런타임에 프로젝트에서 사용되는 자동 생성된 최적화 에셋입니다.
* `public/icon.png` — 소스 아이콘입니다. 이를 교체하고 `npm run icon`을 실행하여 모든 PWA/데스크톱/모바일 변형을 재생성하세요.
* `src/assets/index.ts` — 엔진을 위해 자동 생성된 에셋 매니페스트를 다시 내보냅니다.

</Accordion>

<Accordion title="사용자 인터페이스 (UI)" id="project-structure-ui">

* `src/components/` — React 사용자 인터페이스(UI) 컴포넌트(화면, 메뉴, 대화 상자 등)입니다.
* `src/routes/` — [TanStack Router](https://tanstack.com/router) 라우트 정의입니다. <DynamicLink href="/start/interface-navigate">Navigation</DynamicLink>을 참고하세요.
* `src/styles.css` — 전역 스타일시트([shadcn/ui](https://ui.shadcn.com/))입니다. <DynamicLink href="/start/interface-font">Fonts & styles</DynamicLink>을 참고하세요.

</Accordion>

<Accordion title="번역" id="project-structure-translations">

* `src/lib/i18n.ts` — 국제화 설정입니다. <DynamicLink href="/start/translate">Translations</DynamicLink>을 참고하세요.
* `src/locales/` — 언어별 번역 JSON 파일입니다(예: `en.json`). <DynamicLink href="/start/translate">Translations</DynamicLink>을 참고하세요.

</Accordion>

<Accordion title="초기 설정" id="project-structure-initial-setup">

* `src/models/` — 사용자 지정 클래스와 TypeScript 타입입니다.
* `src/main.tsx` — application entry point; initialises Pixi’VN.
* `src/pixi-vn.keys.gen.ts` — auto-generated file with TypeScript types for all keys in the Pixi’VN API (e.g. `PixivnCharacterIds`, `PixivnLabelIds`, etc.). 엔진 작업 시 타입 안전성과 자동 완성을 위해 사용됩니다.
* `src/pixi-vn.d.ts` — `@drincs/pixi-vn`에 대한 TypeScript 선언 오버라이드입니다.

</Accordion>

<Accordion title="멀티플랫폼" id="project-structure-multi-platform">

* `src-tauri/` — Rust/Tauri 소스입니다(멀티플랫폼 템플릿 전용). 네이티브 데스크톱 및 모바일 빌드를 가능하게 합니다.

</Accordion>

<Accordion title="기타" id="project-structure-other">

* `src/lib/` — 유틸리티 및 내부 로직입니다:
  * `steam.ts` — Steam API(업적, 클라우드 저장)입니다. <DynamicLink href="/start/distribution-steam">Steam distribution</DynamicLink>을 참고하세요.
* `src/constants.ts` — 프로젝트 전역 상수(화면 크기, 구성 값 등)입니다.

</Accordion>

</Accordions>

### 템플릿 화면

템플릿에는 비주얼 노벨의 필수적인 부분을 다루는 사전 제작된 화면 세트가 포함되어 있습니다. 각 화면은 TanStack Router가 관리하는 라우트에 연결된 React 컴포넌트입니다.

* **Main menu** (`/`) — 플레이어가 게임을 실행할 때 처음 보게 되는 화면입니다. 새 게임을 시작하거나, 저장된 게임을 계속하거나, 설정을 여는 진입점을 제공합니다.
* **Narration screen** (`/game/narration`) — 스토리가 표시되는 핵심 게임플레이 화면입니다. 대화, 캐릭터 스프라이트, 배경, 선택지를 렌더링합니다.
* **Settings** — 설정 패널은 자체적인 전용 라우트를 가지고 있지 않습니다. 대신 [Search Params](https://tanstack.com/router/latest/docs/framework/react/guide/search-params)를 사용하여 현재 활성화된 화면 위에 오버레이됩니다. 이 방식은 URL을 깔끔하게 유지하면서도 플레이어가 게임 어디에서든 설정을 열 수 있게 하고, 있던 위치를 잃지 않으면서 설정 섹션(오디오, 디스플레이, 언어 등) 간을 이동할 수 있게 해줍니다.
* **Navigation screen** (`/game/navigation`) — *Point & Click* 전용입니다. 플레이어가 방 사이를 이동하고 현재 위치에서 이용 가능한 다양한 행동을 수행할 수 있게 해줍니다.
* **Map screen** (`/game/map`) — *Point & Click* 전용입니다. 맵을 보여주고 플레이어가 한 위치에서 다른 위치로 이동할 수 있게 해줍니다. 또한 여러 맵을 관리하는 기능도 지원합니다.
