---
title: Knots
description: |
  Scopri come funzionano nodi ed etichette in Pixi'VN, come eseguirli da script di ink o JavaScript e le differenze tra ink nativo e l'integrazione con Pixi'VN.
---

import { NativeJumpExample, NativeCallExample, JumpExample, CallExample } from "@/components/ink-examples";

Cos'è un **`knot`**? Come spiegato nella [documentazione ufficiale](https://www.inklestudios.com/ink/web-tutorial/), la storia è composta da più sezioni collegate che chiamiamo `knots` nella terminologia **_ink_**. L'inizio di un `knot` viene indicato in **_ink_** utilizzando almeno due segni di uguale sul lato sinistro del nome del `knot` e, facoltativamente, anche sul lato destro (ad esempio `=== london ===`).

Ci sono differenze tra `label` e `knot`? No, sono la stessa cosa. In Pixi'VN, quando si crea un `knot` con **_ink_**, verrà creata internamente un'`label` che verrà salvata nel registro.

## Eseguire un knot

Per eseguire un `knot` puoi usare i seguenti metodi:

<Accordions>

<Accordion title="ink_jump" id="jump">

You can execute a `knot` using the Divert operator, which is represented by the `-> start` syntax. Corrisponde alla funzionalità <DynamicLink href="/start/labels#jump-to-a-label">`jump`</DynamicLink> nella libreria Pixi'VN.

Scrivi il simbolo `->` seguito dal nome del `knot`, ad esempio:

```ink title="ink"
=== start ===
Start
-> after // [!code focus]

=== after ===
After
End
-> DONE
```

<NativeJumpExample />

</Accordion>

<Accordion title="ink_call" id="call">

<Callout type="info" title="Threads">

Unlike **native ink**, in **ink + Pixi’VN integration** "Threads" are not supported, so the `<-` symbol corresponds to calling a `knot` instead of being a "Thread" operator.

</Callout>

<Callout type="info">

For simplicity, in **ink + Pixi’VN integration**, `->->` and `-> DONE` will correctly close the `label`, so they are equivalent.

</Callout>

You can execute a `knot` using the Tunnels operator, which is represented by the `-> start ->` syntax. Corrisponde alla funzionalità <DynamicLink href="/start/labels#call-to-a-label">`call`</DynamicLink> nella libreria Pixi'VN.

Write the `->` symbol followed by the name of the `knot` and then the `->` symbol again, for example:

```ink title="ink"
VAR reputation = 0

-> city
=== city ===
You are in the square.

* [Talk to the guard]
    -> guard_dialogue ->
    You return to the square.
    -> city
    You don't see this line because you are sent back to the city before.

* {reputation >= 1} [Ask for a favor]
    The guard helps you.
    -> END

=== guard_dialogue ===
The guard looks at you.

* [Greet]
    "Hello citizen."
    ~ reputation += 1
->->

* [Insult]
    "Hey you!"
    ~ reputation -= 1

->->
```

<NativeCallExample />

</Accordion>

</Accordions>
