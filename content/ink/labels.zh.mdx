---
title: Knots
description: |
  了解节点（knots）和标签（labels）在Pixi'VN中如何工作，如何通过ink脚本或 JavaScript运行它们，以及原生ink与Pixi'VN集成的ink之间有何区别。
---

import { NativeJumpExample, NativeCallExample, JumpExample, CallExample } from "@/components/ink-examples";

什么是**节点（`knot`）**？ 正如[官方文档](https://www.inklestudios.com/ink/web-tutorial/)所解释的那样，故事由多个相互链接的部分组成，在**_ink_术语**中我们将其称之为“节点”（`knot`）。 在 **_ink_** 中，节点（`knot`）（名称的左侧至少使用两个等号，右侧也可以加上等号（例如：`=== london ===`）。

标签（`label`）和节点（`knot`）有区别吗？ 没有，它们是同一个东西。 在 Pixi'VN 中，当你用 **_ink_** 创建一个节点（`knot`）时，内部将会创建一个“标签”，并将其保存在注册表中。

## 运行一个节点（`knot`）

要运行一个节点，你可以使用以下方法：

<Accordions>

<Accordion title="ink_jump" id="jump">

You can execute a `knot` using the Divert operator, which is represented by the `-> start` syntax. 它与Pixi'VN库中的<DynamicLink href="/start/labels#jump-to-a-label"> `jump`功能</DynamicLink>相对应。

编写 `->` 符号，后接节点（`knot`）的名称，例如：

```ink title="ink"
=== start ===
Start
-> after // [!code focus]

=== after ===
After
End
-> DONE
```

<NativeJumpExample />

</Accordion>

<Accordion title="ink_call" id="call">

<Callout type="info" title="Threads">

Unlike **native ink**, in **ink + Pixi’VN integration** "Threads" are not supported, so the `<-` symbol corresponds to calling a `knot` instead of being a "Thread" operator.

</Callout>

<Callout type="info">

For simplicity, in **ink + Pixi’VN integration**, `->->` and `-> DONE` will correctly close the `label`, so they are equivalent.

</Callout>

You can execute a `knot` using the Tunnels operator, which is represented by the `-> start ->` syntax. 你可以使用->符号来跳转到一个节点（`knot`）。这相当于 Pixi'VN 库中的<DynamicLink href="/start/labels#call-to-a-label">调用（`call` ）</DynamicLink>功能。

Write the `->` symbol followed by the name of the `knot` and then the `->` symbol again, for example:

```ink title="ink"
VAR reputation = 0

-> city
=== city ===
You are in the square.

* [Talk to the guard]
    -> guard_dialogue ->
    You return to the square.
    -> city
    You don't see this line because you are sent back to the city before.

* {reputation >= 1} [Ask for a favor]
    The guard helps you.
    -> END

=== guard_dialogue ===
The guard looks at you.

* [Greet]
    "Hello citizen."
    ~ reputation += 1
->->

* [Insult]
    "Hey you!"
    ~ reputation -= 1

->->
```

<NativeCallExample />

</Accordion>

</Accordions>

## 其他叙事功能

<Accordions>

<Accordion title="parameters" id="parameters">

A particularly useful form of temporary variable is a **parameter**. Any knot or stitch can receive a value as a parameter, which is treated as a local temporary variable inside that knot:

```ink title="ink"
=== start ===
*   [Accuse Hastings]
        -> accuse("Hastings")
*   [Accuse Mrs Black]
        -> accuse("Claudia")
*   [Accuse myself]
        -> accuse("myself")

=== accuse(who) ===
    "I accuse {who}!" Poirot declared.
    "Really?" Japp replied. "{who == "myself":You did it?|{who}?}"
    "And why not?" Poirot shot back.
```

Parameters are the correct way to pass a temporary value from one knot or stitch into another, because ordinary `temp` variables are not accessible outside the stitch where they were declared.

### Recursive knots with parameters

Temporary variables (including parameters) are safe to use in recursive knots:

```ink title="ink"
-> add_one_to_one_hundred(0, 1)

=== add_one_to_one_hundred(total, x) ===
    ~ total = total + x
    { x == 100:
        -> finished(total)
    - else:
        -> add_one_to_one_hundred(total, x + 1)
    }

=== finished(total) ===
    "The result is {total}!" you announce.
    -> END
```

### Sending divert targets as parameters

Knot addresses can also be passed as parameters. Use `->` to indicate that a parameter is a divert target:

```ink title="ink"
=== sleeping_in_hut ===
    You lie down and close your eyes.
    -> generic_sleep(-> waking_in_the_hut)

=== generic_sleep(-> waking) ===
    You sleep perchance to dream etc. etc.
    -> waking

=== waking_in_the_hut ===
    You get back to your feet, ready to continue your journey.
```

</Accordion>

</Accordions>
