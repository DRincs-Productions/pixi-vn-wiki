---
title: Altre caratteristiche narrative
description: |
  Explains features such as `pause` and `continue` and how they affect `steps` and dialogue flow.
---

import { PauseExample, ContinueExample } from "@/components/ink-examples";

## Pause

After executing a "# script", the system performs a <DynamicLink href="/start/labels-flow#continue">`continue`</DynamicLink> to move to the next `step`. Ad esempio, in questo caso vedrai l'immagine e il testo del dialogo.

```ink
=== start ===
# show image alien eggHead
Hello, world!
-> DONE
```

È possibile utilizzare `# pause` per interrompere l'avanzamento al `passo` successivo e cancellare il testo del dialogo finché il flusso non riprende. Questa funzione è utile quando è necessario visualizzare un'immagine o cambiare stato senza visualizzare alcuna finestra di dialogo.

La sintassi è:

```ink
# pause
```

```ink tab="ink/start.ink"
=== start ===
# show image alien eggHead
# pause
Hello, world!
-> DONE
```

```ts tab="assets/manifest.ts"
import { AssetsManifest } from "@drincs/pixi-vn";

/**
 * Manifest for the assets used in the game.
 * You can read more about the manifest here: https://pixijs.com/8.x/guides/components/assets#loading-multiple-assets
 */
const manifest: AssetsManifest = {
    bundles: [
        {
            name: "start",
            assets: [
                {
                    alias: "eggHead",
                    src: "https://pixijs.com/assets/eggHead.png",
                },
            ],
        },
    ],
};
export default manifest;
```

<PauseExample />

## Continue

Normalmente, quando una scelta viene presentata dopo un dialogo, la narrazione utilizza due `steps`: la prima mostra solo il dialogo, e la seconda mostra il dialogo più le scelte.

```ink
=== start ===
Who are we going to rescue: the kitten or the wizard?
* [workplace_midground_kitten] 
    -> END
* [workplace_midground_wizard]
    -> END
```

Per presentare il dialogo e le scelte in un unico `step`, aggiungere `<># continue` alla riga del dialogo. This avoids an extra `step`.

```ink
=== start ===
Who are we going to rescue: the kitten or the wizard?<># continue
* [workplace_midground_kitten] 
    -> END
* [workplace_midground_wizard]
    -> END
```

<ContinueExample />
